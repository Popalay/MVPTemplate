package ${packageName};

import com.arellomobile.mvp.presenter.InjectPresenter;
 
public class ${className}Activity extends BaseActivity implements ${className}View {

	@InjectPresenter
	${className}Presenter mPresenter;
}
